# B2C Composition-Heavy Example

Only consider `widgets` when repeated, meaningful, cross-feature blocks appear.

Example candidates:

- `TripSearchSummary`
- `CheckoutSummaryCard`
- `CourseEnrollmentSummary`

Even here, start with feature-local components and thin screen composition first. Introduce `widgets` only after repetition and ownership ambiguity are both real.
