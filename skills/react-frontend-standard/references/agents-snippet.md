# AGENTS.md Snippet

```md
## Shared React Frontend Standard

This repository uses the local skill `react-frontend-standard` together with `ARCHITECTURE.md` and `docs/coding-patterns.md`.

- Use the local project documents for normal feature work.
- Use `react-frontend-standard` explicitly when bootstrapping a new React repository, aligning structure, or regenerating standards documents.
- If the shared standard and local docs conflict, local docs win.
```
